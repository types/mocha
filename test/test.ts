/// <reference path="../out/main.d.ts" />

describe('describe', () => {
  before('before', () => {
  });

  after('after', () => {
  });

  it('simple test', () => {
  });

  it.only('Exclusive test', () => {
  });

  it.skip('Skip test', () => {
  });

  it('pending test');
});
