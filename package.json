{
  "private": true,
  "scripts": {
    "build": "typings bundle -o out --ambient",
    "test": "mocha --require ts-node/register test/test.ts"
  },
  "devDependencies": {
    "blue-tape": "^0.2.0",
    "mocha": "2.4.5",
    "ts-node": "^0.7.0",
    "tslint": "github:unional/tslint",
    "tslint-config-typings": "^0.1.1",
    "typescript": "^1.8.9",
    "typings": "^0.7.9"
  }
}
